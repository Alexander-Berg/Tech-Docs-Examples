feature: Главная страница

specs:
    Заголовок:
        - do: перейти по адресу /adv
        - assert: заголовок во вкладке браузера равен 'adv'
        - assert: на странице есть заголовок красного цвета с текстом "Hello home page, it's touch"

# v-team: your team

files:
    - tests/hermione/suites/touch/home/home.hermione.ts
