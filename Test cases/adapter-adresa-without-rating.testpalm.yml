feature: Адресный сниппет
type: Без рейтинга

specs:
  Без оффера:
    - params:
        foreverdata: '3256244066'
    - do: получить выдаче по запросу 'foreverdata'
    - assert: в результатах поиска есть сниппет с количеством отзывов и рейтингом
    - screenshot: пример внешнего вида выдачи [no-offer]

  С офферным рейтингом:
    - params:
        foreverdata: '1475995166'
        exp_flags:
          - direct_greenurl_ratings_and_reviews=1
    - do: получить выдаче по запросу 'foreverdata'
    - assert: в результатах поиска нет сниппетов с количеством отзывов и рейтингом в описании
    - screenshot: пример внешнего вида выдачи [rating]

  С офферной тумбой:
    - params:
        foreverdata: '1512868615'
        exp_flags:
          - direct_offer_thumb=1
    - do: получить выдаче по запросу 'foreverdata'
    - assert: в результатах поиска есть сниппет с количеством отзывов и рейтингом в описании
    - screenshot: пример внешнего вида выдачи [thumb]

v-team: Distribution

files:
    - features/desktop/address-snippet/adapter-adresa-without-rating/adapter-adresa-without-rating.hermione.js

tags:
  - skip_e2e_check
  - dm_offer
  - no_assessors

tlds: all
