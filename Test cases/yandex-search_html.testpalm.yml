feature: HTML-Шапка

specs:
  beforeEach:
    - do: получить выдачу по запросу 'колендарь'
  Без залогина tld=ru l10n=ru:
    Plain:
      - tlds: ru
      - langs: ru
      - assert: вверху страницы есть шапка
      - assert: в шапке есть навигационные табы сервисов Яндекса
      - screenshot: вид по умолчанию соответствует скриншоту [plain]
  Без залогина tld=kz l10n=kk:
    Plain:
      - tlds: kz
      - langs: kk
      - assert: вверху страницы есть шапка
      - assert: в шапке есть навигационные табы сервисов Яндекса
      - screenshot: вид по умолчанию соответствует скриншоту [plain]
  Без залогина tld=by l10n=be:
    Plain:
      - tlds: by
      - langs: be
      - assert: вверху страницы есть шапка
      - assert: в шапке есть навигационные табы сервисов Яндекса
      - screenshot: вид по умолчанию соответствует скриншоту [plain]
  Без залогина tld=com.tr l10n=tr:
    Plain:
      - tlds: com.tr
      - langs: tr
      - assert: вверху страницы есть шапка
      - assert: в шапке есть навигационные табы сервисов Яндекса
  С залогином  tld=ru l10n=ru:
    Plain:
      - tlds: ru
      - langs: ru
      - assert: вверху страницы есть шапка
      - assert: в шапке есть навигационные табы сервисов Яндекса
      - screenshot: вид по умолчанию соответствует скриншоту [plain]
  С залогином  tld=kz l10n=kk:
    Plain:
      - tlds: kz
      - langs: kk
      - assert: вверху страницы есть шапка
      - assert: в шапке есть навигационные табы сервисов Яндекса
      - screenshot: вид по умолчанию соответствует скриншоту [plain]
  С залогином  tld=by l10n=be:
    Plain:
      - tlds: by
      - langs: be
      - assert: вверху страницы есть шапка
      - assert: в шапке есть навигационные табы сервисов Яндекса
      - screenshot: вид по умолчанию соответствует скриншоту [plain]
  С залогином  tld=com.tr l10n=tr:
    Plain:
      - tlds: com.tr
      - langs: tr
      - assert: вверху страницы есть шапка
      - assert: в шапке есть навигационные табы сервисов Яндекса
      - screenshot: вид по умолчанию соответствует скриншоту [plain]

files:
  - features/desktop/yandex-search/yandex-search_html.hermione.js

v-team: Lego

priority: high

tlds: none
