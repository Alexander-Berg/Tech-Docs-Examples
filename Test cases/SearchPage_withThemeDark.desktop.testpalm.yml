feature: SearchPage
experiment: Темная тема

params:
  exp_flags:
    - exp_flags=dark_theme_desktop=dark

specs:
  Шапка:
    beforeEach:
      - do: открыть https://yandex.ru/products/search?text=iphone
      - assert: |
          открылась страница выдачи
          выдача соответствует запросу
          вверху страницы отображается поисковое поле с текстом запроса
    Внешний вид:
      Лого:
        - assert: слева от поискового поля отображается лого "Яндекс"
        - screenshot: лого "Яндекс" [logo]

v-team: goods

files:
    - src/pages/SearchPage/tests/exp/SearchPage_withThemeDark.desktop.hermione.ts
