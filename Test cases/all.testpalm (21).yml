feature: select

description: Блок select — раскрывающийся список выбора. Каждый пункт списка создается с помощью элемента блока option, который должен быть вложен в контейнер select. В HTML всегда лежит невидимый select для использования его функциональности. При отключенном JS появляется нативный контрол. Раскрывающийся список имеет всего один уровень вложенности, как в нативном выпадающем списке, и позволяет выбрать только один элемент из списка. Больше уровней вложенности не предусмотрено. select — составной блок, его реализация основана на блоках button (кнопка блока) и popup (всплывающее окно блока). Визуально представлен кнопкой и выпадающим списком. В версиях браузера MSIE ниже 8 блок select деградируют до нативного.

specs:
  Проверить корректность работы примера №1(C шириной auto):

    - do: Проверить корректность верстки и состав примера.
    - assert: При наведении курсора мыши на кнопку, рамка кнопки становится темнее, если убрать курсор мыши, то рамка возвращается в исходное состояние?
    - assert: Пример состоит из двух выпадающих списков разного размера, списки примера отличают размером кнопки и размером шрифтов(как бы странно это не звучало)?
    - assert: Внешний вид примеров корректный, состав примера и внешний вид соответствует предыдущей версии библиотеки?
    - assert: В примерах по умолчанию выбран первый элемент из списка, который открывается по клику на кнопку?
    - assert: При нажатии на кнопку, открывается выпадающий список выбранный элемент в списке подсвечивается желтым и слева стоит галочка?
    - assert: По клику на элемент выпадушки она закрывается, а кнопка меняет свою ширину по длине текста выбранного элемента?
    - assert: Если открыть выпадушку и кликнуть за пределами выпадушки, то выпадушка закроется?
    - assert: Если открыть выпадушку, переместить фокус на другой элемент примера при помощи клавиши Tab, то выпадушка вызванная по клику мыши, закроется?
    - assert: Если вызвать выпадушку по клику мыши или при помощи клавиатуры и нажать клавишу Esc, то выпадушка пропадает с экрана?

  Проверить корректность работы примера №2(С ленивой инициализацией):

    - do: Проверить корректность верстки и состав примера.
    - assert: При наведении курсора мыши на кнопку, рамка кнопки становится темнее, если убрать курсор мыши, то рамка возвращается в исходное состояние?
    - assert: Внешний вид примеров корректный, состав примера и внешний вид соответствует предыдущей версии библиотеки?
    - assert: В примерах по умолчанию выбран первый элемент из списка, который открывается по клику на кнопку?
    - assert: При нажатии на кнопку, открывается выпадающий список выбранный элемент в списке подсвечивается желтым и слева стоит галочка?
    - assert: По клику на элемент выпадушки она закрывается, а кнопка меняет свою ширину по длине текста выбранного элемента?
    - assert: Если открыть выпадушку и кликнуть за пределами выпадушки, то выпадушка закроется?
    - assert: Если открыть выпадушку, переместить фокус на другой элемент примера при помощи клавиши Tab, то выпадушка вызванная по клику мыши закроется?
    - assert: Если вызвать выпадушку по клику мыши или при помощи клавиатуры и нажать клавишу Esc, то выпадушка пропадает с экрана?

  Проверить корректность работы примера №3(C шириной auto и max-width):

    - do: Проверить функциональность блока когда ширина блока выбирается автоматически, а у выпадушки задана максимальная ширина.
    - assert: При выборе какого-либо из элементов выпадушки ширина кнопки меняет свою ширину по длине текста выбранного элемента?
    - assert: Текст одного из элементов выпадушки настолько большой, что он не помещается в выпадушку из-за ограничения по ширине выпадушки, не влезающий текст в этом элементе замещается многоточием?
    - assert: При выборе элемента выпадушки, который не помещается по ширине в кнопке отображается тот же текст, что и в выпадушке (различия в два-три символа считаются допустимыми)?

  Проверить корректность работы примера №4(С фиксированной шириной):

    - do: Проверить функциональность блока когда ширина блока фиксирована.
    - assert: Вне зависимости от того, элемент какой длины выбран в выпадушке, длина кнопки не изменяется?

  Проверить корректность работы примера №5(Неактивный селект c фиксированной шириной):

    - do: Проверить функциональность(или отсутствие функциональности) блока в состоянии disabled.
    - assert: Блок в состоянии disabled не реагирует на нажатия, на блок в состоянии disabled нельзя поставить фокус при помощи клавиатуры?

  Проверить корректность работы примера №6(C разной фиксированной шириной в размерах):

    - do: Проверить функциональность блока, когда ширина блока фиксирована.
    - assert: Вне зависимости от того, элемент какой длины выбран в выпадушке, длина кнопки не изменяется?
    - assert: Текст элементов выпадушки настолько большой, что он не помещается в выпадушку из-за ограничения по ширине выпадушки, не влезающий текст в этом элементе замещается многоточием?
    - assert: При выборе элемента выпадушки, который не помещается по ширине в кнопке отображается тот же текст, что и в выпадушке т.е. текст не влезающий по ширине так же замещается многоточием (различия в два-три символа считаются допустимыми)?

  Проверить корректность работы примера №7(C неактивными пунктами в выпадающем списке):

    - do: Проверить работу элементов выпадушки в состоянии disabled.
    - assert: Элементы с текстом ТВЦ и National Geographic отмечены как элементы в состоянии disabled?
    - assert: Элементы выпадушки в состоянии disable отмечены серым?
    - assert: Элементы выпадушки в состоянии disable нельзя выбрать при помощи мыши?
    - assert: Элементы выпадушки в состоянии disable нельзя выбрать при помощи клавиатуры?

  Проверить корректность работы примера №8(C overflow-y и фиксированной шириной):

    - do: Проверить работу выпадушки с фиксированной высотой и шириной.
    - assert: Если открыть выпадушку, то видимая ее часть состоит из 5 строк, полная выпадушка из 10?
    - assert: Выпадающий список можно проскроллить, что бы увидеть весь список элементов представленных в выпадушке?
    - assert: Текст элементов выпадушки настолько большой, что он не помещается в выпадушку из-за ограничения по ширине выпадушки, не влезающий текст в этом элементе замещается многоточием?
    - assert: При выборе элемента выпадушки, который не помещается по ширине, в кнопке отображается тот же текст, что и в выпадушке т.е. текст не влезающий по ширине так же замещается многоточием (различия в два-три символа считаются допустимыми)?
    - assert: Вне зависимости от того, элемент какой длины выбран в выпадушке, длина кнопки не изменяется?

  Проверить корректность работы примера №9(C overflow-y и шириной auto):

    - do: Проверить работу выпадушки с фиксированной высотой и автоматической шириной.
    - assert: Видимая часть выпадушки состоит из пяти строк?
    - assert: Выпадающий список можно проскроллить, что бы увидеть весь список элементов представленных в выпадушке?
    - assert: В зависимости от того, элемент какой длины выбран в выпадушке, длина кнопки изменяется по ширине выбранного текста?
    - assert: По клику на элемент выпадушки она закрывается, а кнопка меняет свою ширину по длине текста выбранного элемента?

  Проверить корректность работы примера №12(Динамически перестраиваемые селекты (1), проброс модификаторов блоку popup и элементу select__popup):

    - do: Проверить, что значение селектора «город» меняется в зависимости от значения селектора «страна».
    - assert: При выборе значения «Россия» в селекторе «страна», в селекторе «город» к выбору доступны значения «Москва», «Санкт-Петербург», «Красноярск»?
    - assert: При выборе значения «Украина» в селекторе «страна», в селекторе «город» к выбору доступны значения «Симферополь», «Севастополь», «Керчь»?
    - assert: При выборе значения «Беларусь» в селекторе «страна», в селекторе «город» к выбору доступны значения «Минск», «Брест»?

  Проверить корректность работы примера №13(Динамически перестраиваемые селекты (2)):

    - do: Проверить, что значение селектора «город» меняется в зависимости от значения селектора «страна».
    - assert: При выборе значения «Россия» в селекторе «страна», в селекторе «город» к выбору доступны значения «Москва», «Санкт-Петербург», «Красноярск»?
    - assert: При выборе значения «Украина» в селекторе «страна», в селекторе «город» к выбору доступны 12 городов + 1 в состоянии disabled?
    - assert: При выборе значения «Беларусь» в селекторе «страна», в селекторе «город» к выбору доступны значения «Минск», «Витебск», «Брест»?

  Проверить корректность работы примера №14(В попапе):

    - do: Проверить работу select внутри попапчика.
    - assert: По клику на ссылку Кликнуть сюда открывается выпадушка с селектом?

    - do: Проверить корректность работы селекта внутри выпадушки.
    - assert: Внешний вид примеров корректный, состав примера и внешний вид соответствует предыдущей версии библиотеки?
    - assert: В примерах по умолчанию выбран первый элемент из списка, который открывается по клику на кнопку?
    - assert: При нажатии на кнопку открывается выпадающий список, выбранный элемент в списке подсвечивается желтым и слева стоит галочка?
    - assert: По клику на элемент выпадушки она закрывается, а кнопка меняет свою ширину по длине текста выбранного элемента?
    - assert: Если открыть выпадушку и кликнуть за пределами выпадушки, то выпадушка закроется вместе с попапчиком?
    - assert: Если вызвать выпадушку по клику мыши или при помощи клавиатуры и нажать клавишу Esc, то выпадушка пропадает с экрана, но попапчик с селектом останется на экране?


  Проверить корректность работы примера №15(В popup2):

    - do: Проверить работу select внутри попапчика.
    - assert: По клику на ссылку Кликнуть  сюда открывается выпадушка с селектом?

    - do: Проверить корректность работы селекта внутри выпадушки.
    - assert: Внешний вид примеров корректный, состав примера и внешний вид соответствует предыдущей версии библиотеки?
    - assert: В примерах по умолчанию выбран первый элемент из списка, который открывается по клику на кнопку?
    - assert: При нажатии на кнопку открывается выпадающий список, выбранный элемент в списке подсвечивается желтым и слева стоит галочка?
    - assert: По клику на элемент выпадушки она закрывается, а кнопка меняет свою ширину по длине текста выбранного элемента?
    - assert: Если открыть выпадушку и кликнуть за пределами выпадушки, то выпадушка закроется вместе с попапчиком?
    - assert: Если открыть выпадушку, переместить фокус на другой элемент примера при помощи клавиши Tab, то выпадушка вызванная по клику мыши закроется?
    - assert: Если вызвать выпадушку по клику мыши или при помощи клавиатуры и нажать клавишу Esc, то выпадушка пропадает с экрана, но попапчик с селектом останется на экране?

  Проверить корректность работы примера №16(В попапе с position fixed):

    - do: Проверить работу select внутри попапчика.
    - assert: По клику на ссылку Кликнуть сюда открывается выпадушка с селектом?

    - do: Проверить корректность работы селекта внутри выпадушки.
    - assert: При наведении курсора мыши на кнопку, рамка кнопки становится темнее, если убрать курсор мыши, то рамка возвращается в исходное состояние?
    - assert: Внешний вид примеров корректный, состав примера и внешний вид соответствует предыдущей версии библиотеки?
    - assert: В примерах по умолчанию выбран первый элемент из списка, который открывается по клику на кнопку?
    - assert: При нажатии на кнопку открывается выпадающий список, выбранный элемент в списке подсвечивается желтым и слева стоит галочка?
    - assert: По клику на элемент выпадушки она закрывается, а кнопка меняет свою ширину по длине текста выбранного элемента?
    - assert: Если открыть выпадушку и кликнуть за пределами выпадушки, то выпадушка закроется вместе с попапчиком?
    - assert: Если открыть выпадушку, переместить фокус на другой элемент примера при помощи клавиши Tab, то выпадушка вызванная по клику мыши закроется?
    - assert: Если вызвать выпадушку по клику мыши или при помощи клавиатуры и нажать клавишу Esc, то выпадушка пропадает с экрана, но попапчик с селектом останется на экране?

  Проверить корректность работы примера №17(Выравнивание относительно кнопок):

    - do: Проверить корректность верстки примера.
    - assert: Текст внутри кнопки селекта расположен на одной линии?

  Проверить корректность работы примера №18(Очень длинный список с ограничением количества строк):

    - do: Это пример селекта в котором очень много значений и он не должен ломаться из-за большого числа значений.
    - assert: Если нажать на пример, то открывается выпадушка из 5 строчек?
    - assert: Выпадушка имеет 100 строк, пронумерованных от 0 до 99?
    - assert: Выпадушку модно проскроллить в начало и в конец, первый и последний элемент можно выбрать?
    - assert: Полоса прокрутки не портит пример т.е. не наезжает на текст, не портит внешний вид выпадушки, не загораживает текст в выпадушке?

files: []

v-team: lego

example: /all/all.html

manager:
  - kuznecov

qa-engineer:
  - spiker

priority:
  - high

tags:
  - forms
  - bem
