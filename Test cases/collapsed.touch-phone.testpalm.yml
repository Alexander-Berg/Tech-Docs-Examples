feature: Состояние кнопки на тачах

specs:
  before:
    - do: открыть тестовый стенд с виджетом

  Состояния кнопки корректно отображаются на тачах:
    - do: выставить параметр конструктора collapsedTouch=never
    - screenshot: кнопка виджета развернута [collapsed-never]
    - do: выставить параметр конструктора collapsedTouch=always
    - screenshot: кнопка виджета свернута [collapsed-always]

files:
  - tests/hermione/suites/widget/collapsed/collapsed.touch-phone.hermione.js

v-team: Chat

tags:
    - no_assessors
    - widget

tlds: all
