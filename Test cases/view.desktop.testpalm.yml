feature: Внешний вид инпута

import-steps:
    - tests/hermione/suites/messenger/shared-steps/open-messenger.testpalm.steps.yml

tlds: ru

specs:

    Скролл в инпуте:
        - attach: Открытие чата
        - do: кликнуть в поле ввода
        - do: в поле ввода ввести произвольный текст больше 7 строк
        - assert: |
            первая строка уезжает под верхнюю часть поля ввода
            поле ввода можно скроллить
        - do: проскроллить вверх текст в поле ввода
        - assert: поле ввода скроллится

files:
    - tests/hermione/suites/messenger/sending-messages/view.desktop.hermione.ts

v-team: Chat

tags:
    - chamb
    - yamb
    - y-team
    - no_assessors
