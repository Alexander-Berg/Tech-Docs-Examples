feature: Popup на СЕРПе
type: С отложенным появлением

params:
  foreverdata: '2527250467'
  srcskip: YABS_DISTR

specs:
  beforeEach:
    - do: получить выдачу по запросу 'foreverdata'
    - assert: попап-блок дистрибуции скрыт
  Клик в сниппет:
    - do: кликнуть в сниппет
    - assert: в новой вкладке откроется результат
    - do: закрыть вкладку и вернуться на выдачу
    - screenshot: попап-блок дистрибуции показался на выдаче [tricky]
    - tech: сработал счетчик /tech/promo_popup/delayed/show

files:
  - src/features/DistrPopup/DistrPopup.test/_tricky/DistrPopup_tricky@desktop.hermione.js

tags:
  - skip_e2e_check
  - distribution
  - popup

v-team: Distribution

tlds: [ru, by, ua, kz]
