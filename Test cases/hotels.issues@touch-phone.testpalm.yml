feature: Hotels / Колдунщик многих организаций
type: Стандартный вид

specs:
  Тесты на фиксы багов:
    # Тикет: https://st.yandex-team.ru/SERP-84898
    # no_assessors потому что карту убрали из сайдблока, кейс не нужен
    При повторном открытии сайдблока список отелей перезапрашивается:
      - tags:
          - no_assessors
      - do: получить выдачу по запросу 'отели в ялте'
      - assert: появился колдунщик отелей
      - do: кликнуть в тайтл колдунщика
      - assert: открылся сайдблок
      - assert: в сайдблоке есть спинер на белом фоне
      - assert: запустился поиск по отелям
      - assert: открылся список отелей
      - do: вернуться назад к выдаче
      - do: кликнуть в тайтл колдунщика
      - assert: открылся сайдблок
      - assert: в сайдблоке есть спинер на белом фоне
      - assert: запустился поиск по отелям
      - assert: открылся список отелей под картой
      - assertMetrics: '-'
    # Тикет: https://st.yandex-team.ru/SERP-92123
    При входе в сайдблок списка подгрузка пинов работает:
      - tags:
          - orgmn
          - sideblock
          - offers
          - map
          - switch
      - do: получить выдачу по запросу 'отели в питере'
      - assert: появился колдунщик отелей
      - do: кликнуть в тайтл колдунщика
      - assert: открылся сайдблок со списком отелей
      - do: тапнуть по кнопке 'Карта' переключателя 'Карта/Список'
      - assert: список организаций исчез
      - assert: на карте отображаются пины
      - do: драгнуть карту
      - assert: пины обновились
      - assertMetrics: '-'

files:
  - features/companies/hotels/hotels.issues@touch-phone.hermione.js

counter: /snippet/companies/travel_map

v-team: KotTeam

tags:
  - skip_e2e_check

tlds: none
