feature: Feature one

params:
  foreverdata: '1234'

specs:
  Describe in specs:
    The first it case:
      - params:
          text:
            - something
          exp_flags:
            - font-size=10
      - do: something 1
    The second it case:
      - params:
          text:
            - something2
          exp_flags:
            - hideads=1
            - font-size=10
      - do: something 2
    The last it case:
      - params:
          exp_flags:
            - reviews=1
            - url_font-size=20
      - do: something 3

files:
  - test/tide-mover/fixtures/move-file-to-dir-input/source/test.hermione.js
