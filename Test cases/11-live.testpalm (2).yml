
feature: share-dropdown

description: Блок share-dropdown служит для блока Поделиться БЭМ-оберткой на странице. Реализован блоком dropdown-menu. Он состоит из кнопки Поделиться (блок button) и всплывающего окна (блок popup) со списком популярных социальных сервисов. Текст на кнопке Поделиться и названия сервисов постоянны и автоматически локализуются. Если язык для текста блока явно не задан, то он будет определен по доменной зоне сайта.

specs:
  Проверить корректность работы примера №2(live):
    - do: Проверить корректность отображения примера
    - assert: Пример состоит из двух кнопок 'Поделиться' с блоком 'поделяшек' (ссылок на социальные сети с возможностью поделиться публикацией; у каждого из блоков разный тип инициализации)
    - do: Нажать на каждую ссылку социальной сети в поделяшках
    - assert: По тапу на кнопку под ней появляется выпадающее меню с 'поделяшкой' Facebook
    - assert: по тапу на ссылку открывается страница Facebook
    - do: При открытом выпадающем меню повернуть экран устройства
    - assert: Меню остаётся расположенным так же относительно вызвавшей его кнопки

files: []

example: /11-live/11-live.html


v-team: lego

manager:
  - kuznecov

qa-engineer:
  - spiker

priority:
  - high

tags:
  - bem
