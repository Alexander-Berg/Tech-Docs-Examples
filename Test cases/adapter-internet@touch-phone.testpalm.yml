feature: Факт
type: Интернет (горизонтальный вид)

specs:
  Загрузка адреса по ajax - горизонтальная ориентация:
    - do: получить выдачу Поиска по запросу 'мой ip'
    - assert: на первой позиции выдачи есть Фактовый ответ с ip-адресом
    - do: повернуть устройство в ландшафтную (горизонтальную) ориентацию
    - assert: загрузился адрес ipv4 (адрес ipv6 может отсутствовать)
    - screenshot: горизонтальный вид [plain-horizontal]
    - assertMetrics: '*'

files:
  - src/features/Internet/Internet.test/adapter-internet@touch-phone.hermione.js

v-team: Facts

counter: /$page/$main/$result[@wizard_name="my_ip"]

tags: skip_e2e_check #тоже что и fact-internet

tlds: [ru, by, ua, kz, uz, com.tr, com]
