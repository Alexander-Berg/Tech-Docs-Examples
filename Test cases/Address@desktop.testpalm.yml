feature: Контакты организации

specs:
  beforeEach:
    - do: получить выдачу по запросу 'отель высоцкий'
    - assert: в правой колонке есть колдунщик организации

  Адрес:
    Должен открыться попап с открытой вкладкой "На карте":
      - do: кликнуть в адрес организации
      - assert: открылся попап с активной вкладкой 'На карте'

    Должна открыться вкладка "На карте" при клике в адрес в попапе:
      - do: кликнуть в заголовок организации
      - assert: открылся попап с активной вкладкой 'Про отель'
      - do: кликнуть в адрес организации в попапе
      - assert: открылась вкладка 'На карте'

files:
  - src/features/Companies/Companies.features/OrgContacts/OrgContacts.test/Address/Address@desktop.hermione.js

v-team: VanillaGeo

counter: /$page/$parallel/$result[@wizard_name="companies" and @subtype="company"]/composite/tabs/about/contacts

tags: skip_e2e_check

tlds: all

