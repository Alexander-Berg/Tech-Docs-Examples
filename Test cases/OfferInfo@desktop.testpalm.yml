feature: Офферы
type: Офферное описание

specs:
  Внешний вид:
    - tags:
        - smoke
    - params:
        foreverdata: '1278455664'
    - do: получить выдачу по запросу 'foreverdata'
    - assert: на выдаче есть сниппет с ценой и информацией о доставке
    - screenshot: внешний вид соответствует скриншоту [offer-info]

  Оффер с тумбой:
    - params:
        foreverdata: '1278455664'
    - do: получить выдачу по запросу 'foreverdata'
    - assert: |
        на выдаче есть сниппет с картинкой
        у него есть цена и информация о доставке
    - screenshot: внешний вид соответствует скриншоту [offer-thumb]

  Собственная ссылка у тумбы:
    - params:
        foreverdata: '1278455664'
    - do: получить выдачу по запросу 'foreverdata'
    - assert: на выдаче есть сниппет с тумбой
    - do: кликнуть в заголовок сниппета
    - assert: открылся сайт moscowfresh.ru
    - do: кликнуть в тумбу в сниппете
    - assert: открылся сайт ya.ru

files:
  - src/features/OfferInfo/OfferInfo.test/OfferInfo@desktop.hermione.js

v-team: Distribution

tags:
  - skip_e2e_check
  - dm_offer

tlds: ru
