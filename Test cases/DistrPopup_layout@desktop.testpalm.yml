feature: Дистрибуционный попап — дизайн

specs:
  Дизайн - flat:
    - params:
        exp_flags: distr_splashscreen_disable=1
        foreverdata: '3649741961'
    - do: почистить куки
    - do: получить выдачу по запросу "foreverdata"
    - screenshot: пример внешнего вида попап-блока [flat]
  Дизайн - simple:
    - params:
        exp_flags: distr_splashscreen_disable=1
        foreverdata: '1437395227'
    - do: почистить куки
    - do: получить выдачу по запросу "foreverdata"
    - screenshot: пример внешнего вида попап-блока [simple]
  Дизайн - unknown:
    - params:
        exp_flags: distr_splashscreen_disable=1
        foreverdata: '2229475393'
    - do: почистить куки
    - do: получить выдачу по запросу "foreverdata"
    - screenshot: пример внешнего вида попап-блока [unknown]
  Клик на кнопку отмены:
    - params:
        exp_flags: distr_splashscreen_disable=1
        foreverdata: '3649741961'
    - do: почистить куки
    - do: получить выдачу по запросу "foreverdata"
    - assert: на странице есть попап дистрибуции
    - do: нажать на кнопку закрытия попапа
    - tech: сработал счётчик path="/$page/promo_popup/close[@tags@close=1 and @behaviour@type="dynamic"]"
    - assert: дистрибуционный попап скрывается
  Клик на кнопку установки:
    - params:
        exp_flags: distr_splashscreen_disable=1
        foreverdata: '3649741961'
    - do: почистить куки
    - do: получить выдачу по запросу "foreverdata"
    - assert: на странице есть попап дистрибуции
    - do: нажать на кнопку установки в попапе
    - tech: сработал счётчик path="/$page/promo_popup/install"
    - assert: началась загрузка файла

files:
  - features/desktop/distr-popup/_layout/DistrPopup_layout@desktop.hermione.js

v-team: Distribution

tags:
  - distribution
  - popup
  - no_assessors # убрать после https://st.yandex-team.ru/SERPTEST-2384

tlds:
  - ru
