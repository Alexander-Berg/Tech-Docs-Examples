feature: popup

description: Абстрактный popup – компонент, предназначенный для создания как выпадающего элемента (контекстный попап), так и модального окна. Блок отображается на первом плане и перекрывает остальные элементы на странице. Может служить основой для других блоков. Например, Домик, Саджест. Попап может быть вызван различными элементами страницы, например, иконками, псевдо-ссылками. При использовании хвостика он всегда будет указывать на вызвавший его элемент, что делает очевидной их взаимосвязь. ВНИМАНИЕ! Элементы примера в шапке могут некорректно работать при скролле страницы(могут смешиваться с другими примерами или текст и кнопки будут отображаться поверх элементов шапки) это ожидаемая ошибка.

specs:

  Проверить корректность работы примера №13(fixed):

   (1):

    - do: перезагрузить страницу
    - assert: на странице есть кнопка с надписью 'Показать popup' (наверху слева) 
    - assert: на странице (примерно в центре) есть большой попап с текстом и кнопкой 'Ещё popup' 
    - do: кликнуть на кнопку 'Ещё popup' внутри большого попапа
    - assert: внутри большого попапа появился малелький попап с надписью 'Iʼm popup'
    - do: кликнуть в малелький попап с надписью 'Iʼm popup'
    - assert: ничего не произошло, вид попапов не изменился
    - do: кликнуть в любое место страницы примера вне кнопок и попапов
    - assert: все попапы пропали
    - do: кликнуть на кнопку 'Показать popup'
    - assert: снова показался большой попап

   (2):

    - do: перезагрузить страницу
    - assert: на странице снова есть большой попап с текстом и кнопкой 'Ещё popup'
    - do: кликнуть на кнопку 'Ещё popup' внутри большого попапа
    - assert: внутри большого попапа появился малелький попап с надписью 'Iʼm popup'
    - do: кликнуть в текст большого попапа (не касаясь кнопки 'Ещё popup' и крестика в верхнем правом углу)
    - assert: малелький попап с надписью 'Iʼm popup' исчез

   (3):

    - do: перезагрузить страницу
    - assert: на странице снова есть большой попап с текстом и кнопкой 'Ещё popup'
    - do: кликнуть на кнопку 'Ещё popup' внутри большого попапа
    - assert: внутри большого попапа появился малелький попап с надписью 'Iʼm popup'
    - do: кликнуть в крестик в верхнем правом углу большого попапа
    - assert: все попапы пропали

   (4):

    - do: перезагрузить страницу
    - assert: на странице снова есть большой попап с текстом и кнопкой 'Ещё popup'
    - do: кликнуть на кнопку 'Ещё popup' внутри большого попапа
    - assert: внутри большого попапа появился малелький попап с надписью 'Iʼm popup'
    - do: ещё раз кликнуть на кнопку 'Ещё popup' внутри большого попапа
    - assert: малелький попап с надписью 'Iʼm popup' исчез
    - do: снова кликнуть на кнопку 'Ещё popup' внутри большого попапа
    - assert: опять появился малелький попап с надписью 'Iʼm popup

   (5):

    - do: перезагрузить страницу
    - assert: на странице снова есть большой попап с текстом и кнопкой 'Ещё popup'
    - do: кликнуть на кнопку 'Ещё popup' внутри большого попапа
    - assert: внутри большого попапа появился малелький попап с надписью 'Iʼm popup'
    - do: нажать на клавиатуре клавишу 'Esc'
    - assert: маленький попап с надписью 'Iʼm popup' исчез
    - do: снова кликнуть на кнопку 'Ещё popup' внутри большого попапа
    - assert: снова появился малелький попап с надписью 'Iʼm popup'
    - do: ещё раз кликнуть на кнопку 'Ещё popup' внутри большого попапа
    - assert: малелький попап с надписью 'Iʼm popup' опять исчез
    - do: ещё раз нажать на клавиатуре клавишу 'Esc'
    - assert: большой попап исчез

files: []

example: /50-fixed/50-fixed.html

v-team: lego

manager:
  - kuznecov

qa-engineer:
  - spiker

priority:
  - high

tags:
  - bem
