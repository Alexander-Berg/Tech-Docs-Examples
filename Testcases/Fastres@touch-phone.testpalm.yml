feature: Колдунщик миниблендера

specs:
  Карусель с текстом внутри карточки:
    params:
      foreverdata: '683525429'
    beforeEach:
      - do: получить выдачу по запросу 'foreverdata'
      - assert: на выдаче присутствует колдунщик с каруселью картинок
    Внешний вид:
      - screenshot: внешний вид [showcase-over]
  Сниппет с приложением:
    params:
      foreverdata: '1931361897'
    beforeEach:
      - do: получить выдачу по запросу 'foreverdata'
      - assert: на выдаче присутствует колдунщик со сниппетом приложения
    Внешний вид:
      - screenshot: внешний вид [with-app]
    Ссылка на блоке с приложением:
      - do: нажать на блок с приложением
      - assert: открылся магазин приложений на нужном приложении или запрос 'открыть эту страницу в магазине приложений'
      - tech: сработал счетчик '/$page/$main/$result/bno/app'

files:
  - src/features/Fastres/Fastres.test/Fastres@touch-phone.hermione.js

tags:
  - skip_e2e_check
  - reviewed

counter: /$page/$main/$result[@wizard_name="fastres"]

v-team: Freshness

tlds: all
