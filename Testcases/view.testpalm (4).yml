feature: Блок для опросов

specs:
  Положительные:
    1. Блок для опросов отображается на странице админки:
      - do: авторизоваться под администратором с включенным блоком для опросов и перейти по ссылке "{{CONNECT_HOST}}/portal/admin"
      - assert: отображается блок для опросов
      - screenshot: внешний вид блока для опросов [plain]
    2. Нажатие на кнопку для прохождения опроса:
      - do: авторизоваться под пользователем с включенным блоком для опросов и перейти по ссылке "{{CONNECT_HOST}}/portal/home"
      - assert: отображается блок для опросов
      - screenshot: внешний вид блока для опросов [plain]
      - do: нажать на желтую кнопку в блоке
      - assert: блок для опросов не отображается
      - do: перейти по ссылке "{{CONNECT_HOST}}/portal/admin"
      - assert: блок для опросов не отображается
  Отрицательные:
    1. Закрытие блока для опросов:
      - do: авторизоваться под пользователем с включенным блоком для опросов и перейти по ссылке "{{CONNECT_HOST}}/portal/home"
      - assert: отображается блок для опросов
      - screenshot: внешний вид блока для опросов [plain]
      - do: нажать на крестик в блоке
      - assert: блок для опросов не отображается
      - do: перейти по ссылке "{{CONNECT_HOST}}/portal/admin"
      - assert: блок для опросов не отображается

files:
  - tests/hermione/suites/survey/view.js

priority: critical

tlds:
 - ru
 - com

tags:
  - reviewed
