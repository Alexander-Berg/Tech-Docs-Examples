feature: Колдунщик погоды

specs-integration:
  Основные проверки:
    - do: получить выдачу по запросу "погода"
    - assert: получена выдача по указанному запросу
    - assert: на выдаче присутствует колдунщик погоды
    - assert: в колдунщике погоды присутсвует тайтл
    - assert: в колдунщике погоды присутсвует появилась текущая температура
    - assert: в колдунщике погоды присутсвует блок с детализацией погоды
    - assert: в колдунщике погоды присутсвует блок с прогнозом по дням
    - counter: /$page/$main/$result[@wizard_name="weather"]/title

specs:
  Внешний вид:
    - tags: smoke_extended
    - params:
        foreverdata: '3478844138'
    - do: получить выдачу по запросу "foreverdata"
    - assert: получена выдача по указанному запросу
    - assert: на выдаче присутствует колдунщик погоды
    - screenshot: примерный внешний вид [ plain ]

files:
  - features/adapter-weather/weather_desktop_sitelinks@deskpad.hermione.js
  - features/adapter-weather/weather_desktop_sitelinks@deskpad.hermione.e2e.js

v-team: Weather

manager:
  - podnebesnaya

tlds:
  - ru
  - by
  - uz
  - kz
  - ua
