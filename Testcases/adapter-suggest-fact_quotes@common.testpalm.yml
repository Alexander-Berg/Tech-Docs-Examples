feature: Факт
type: Саджест (акции)

specs:
  Ссылка в карточке:
    - tags: ['smoke']
    - do: получить выдачу Поиска по запросу 'золото цена москва сейчас' и переключиться на один из доменов - by, ua или kz (в том случае, если изначально был другой), можно поменять домен руками в адресной строке _ALTERNATIVE_SEARCH_QUERIES_
    - assert: на первой позиции выдачи есть Фактовый ответ (саджест факт)
    - screenshot: внешний вид [plain]
    - assertMetrics: '*'
    - do: тапнуть на синий тайтл в Фактовом ответе
    - assert: в новой вкладке открылся сайт Яндекс.Новости
    - tech: проверить серверный счетчик на показ синего тайтла
    - assertMetrics: '*'
  Разбивка на разряды:
    - do: получить выдачу Поиска по запросу 'Dow Jones index' _ALTERNATIVE_SEARCH_QUERIES_
    - assert: на первой позиции выдачи есть Фактовый ответ (саджест факт)
    - screenshot: внешний вид [plain]

specs-integration:
  Проверка показа блоков:
    - do: получить выдачу Поиска по запросу 'цены на нефть' _ALTERNATIVE_SEARCH_QUERIES_
    - assert: на первой позиции выдачи есть Фактовый ответ
    - counter: /$page/$main/$result[@wizard_name="suggest_fact" and @subtype="quotes"]

files:
  - features/adapter-suggest-fact/adapter-suggest-fact_quotes@common.hermione.js
  - features/adapter-suggest-fact/adapter-suggest-fact_quotes@common.hermione.e2e.js

v-team: Facts

counter: /$page/$main/$result[@wizard_name="suggest_fact" and @subtype="quotes"]

tlds:
  - by
  - ua
  - kz
