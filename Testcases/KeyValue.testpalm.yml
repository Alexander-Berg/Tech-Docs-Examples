feature: Key-value

specs:
  beforeEach:
    - do: получить выдачу по запросу 'foreverdata' и тапнуть в заголовок первого сниппета (название заголовка Тестовая новостная станица — турбо)
    - assert: открывается турбо-страница в той же вкладке

  Проверка ссылок в key-value:
    - params:
        foreverdata: '2310953522'
    - screenshot: на странице есть key-value, то есть блок с признаком и его значением, разделенными знаком ":" [plain]
    - tech: должен сработать серверный сечтчик на ссылку (/turbo/link)
    - do: тапнуть по ссылкам в блоке key-value
    - tech: проверить клиентский счетчик в ссылке в key-value (/turbo/link)
    - assert: открывается сайт ya.ru в новой вкладке

files:
  - platform/components/KeyValue/__tests__/KeyValue.hermione.js

v-team: Turbo

manager: tanzwuta

qa-engineer: kleev, k0nratka

tlds: none
