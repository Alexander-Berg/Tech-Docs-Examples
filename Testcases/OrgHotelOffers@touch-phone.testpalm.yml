feature: Hotels / Колдунщик одной организации
type: Стандартный вид

specs:
  Партнеры не дублируются после перезапроса:
    - do: получить выдачу по запросу 'radisson collection' _ALTERNATIVE_SEARCH_QUERIES_
    - assert: на выдаче есть колдунщик одного отеля с блоком 'Цены на номера'
    - do: кликнуть в кнопку "Выбрать номер"
    - assert: открылся оверлей с офферами отелей
    - do: кликнуть в кнопку "Назад" в верхней части оверлея
    - assert: в блоке Цены на номера у всех офферов разные партнеры

files:
  - src/features/Companies/Companies.features/OrgHotelOffers/OrgHotelOffers.test/OrgHotelOffers@touch-phone.hermione.js

v-team: KotTeam

counter: /$page/$main/$result/composite/hotels-form

tags:
  - skip_e2e_check

tlds: all
