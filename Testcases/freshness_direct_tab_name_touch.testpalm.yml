feature: Страница всех объявлений
type: Шапка
experiment: Измененный текст рекламного таба в шапке

params:
  exp_flags: 'freshness_direct_tab_name_touch=1'

specs:
  Внешний вид табика рекламной вертикали:
    - do: получить выдачу по запросу 'playstation'
    - do: в адресной строке браузера добавить после 'search/touch/' параметр 'direct', чтобы получилось search/touch/direct, и нажать Enter
    - assert: выдача обновилась
    - screenshot: внешний вид активного таба в шапке [direct-tab]

browsers:
  notFor:
    - searchapp-phone

files:
  - features/touch-phone/search-ads/freshness_direct_tab_name_touch/freshness_direct_tab_name_touch.hermione.js

tags:
  - skip_e2e_check

tlds:
  - ru

v-team: Freshness
