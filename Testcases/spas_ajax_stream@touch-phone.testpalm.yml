feature: СПАС
experiment: Стриминг аякс запросов

specs:
  Логирование:
    - params:
        exp_flags: spas_ajax_stream=1
    - do: получить выдачу с запросом 'тест'
    - do: выполнить в консоли `BEM.blocks['serp-request'].navigateTo('?text=котики')`
    - assert: перезагрузилась страница с запросом 'тест'
    - tech: сработал счетчик event=tech, type=response-streaming, path=$subresult

files:
  - features/spas_ajax_stream/spas_ajax_stream@touch-phone.hermione.js

v-team: SPAS

tags:
  - skip_e2e_check
  - no_assessors

tlds: all
