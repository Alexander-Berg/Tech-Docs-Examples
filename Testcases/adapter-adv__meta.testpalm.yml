feature: Спец.размещение
type: С метой

specs:
  Базовые проверки:
    - params:
        foreverdata: '1873219025'
        exp_flags: direct_turn_off_stages=1
        data_filter: 'adv'
    - do: получить выдачу по запросу 'foreverdata'
    - assert: рекламный сниппет присутствует на выдаче
    - screenshot: пример внешнего вида [meta]
    - do: кликнуть в ссылку "Контакты"
    - tech: сработал счётчик path '/tech/direct/ad/address/click' vars '-reqid=*,-bid=*,-bsblockid=*,-placement=premium,pos=p0'
  Невалидная ссылка на сайт-визитку:
    - params:
        foreverdata: '3357604967'
        data_filter: 'adv'
    - do: получить выдачу по запросу 'foreverdata'
    - assert: рекламный сниппет присутствует на выдаче
    - do: в рекламном сниппете отсутствует ссылка "Контакты"

files:
  - features/touch-phone/adapter-adv/__meta/adapter-adv__meta.hermione.js

v-team: Distribution

tags:
  - skip_e2e_check
  - adv

tlds: all
