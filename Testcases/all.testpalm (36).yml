feature: checkbox

description: Блок checkbox (чекбокс) создает независимый переключатель. Он позволяет управлять параметром с двумя состояниями — включено и выключено, а также формировать группу чекбоксов для выбора более одного варианта из списка. Реализация блока основана на нативном контроле чекбокс.

specs:
    Проверить корректность верстки примеров:
      - do: Откройте ссылку на пример.
      - assert: Верстка примера отображается корректно.

    Проверить корректность работы примера №1(Theme):
      - do: Проверить реакцию на тап.
      - assert: При тапе на checkbox с темой normal элемент переходит в состояние выбрано, если тапнуть еще раз, то элемент переходит в состояние не выбрано.
      - assert: При тапе на текст рядом с checkbox с темой normal элемент переходит в состояние выбрано, если тапнуть еще раз на текст или сам checkbox, то элемент переходит в состояние не выбрано.
      - assert: При нажатии пальцем на checkbox с темой normal и последующем уводе пальца (не отпуская с экрана) с элемента, элемент остается в состоянии не выбрано.
      - assert: При тапе на checkbox с темой pseudo элемент переходит в состояние выбрано, если тапнуть еще раз, то элемент переходит в состояние не выбрано.
      - assert: При тапе на текст рядом с checkbox с темой pseudo элемент переходит в состояние выбрано, если тапнуть еще раз на текст или сам checkbox, то элемент переходит в состояние не выбрано.
      - assert: При нажатии пальцем на checkbox с темой pseudo и последующем уводе пальца (не отпуская с экрана) с элемента, элемент остается в состоянии не выбрано.

      - do: Проверить работу блока в состояние disabled.
      - assert: Перевести блок в состояние disabled нажав на кнопку tooggle disabled. Элементы стали серыми, при нажатии на элементы и текст около элементов состояние элементов не меняется?

    Проверить корректность работы примера №2(Baseline):
      - do: Проверить внешний вид блока кнопок.
      - assert: Текст и блоки расположены на одной линии?

files: []

v-team: lego

example: /all/all.html

manager:
  - kuznecov

qa-engineer:
  - spiker

priority:
  - high

tags:
  - bem
