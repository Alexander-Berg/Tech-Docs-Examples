feature: Пейджер
type: Пасхалка для колдунщика stackoverflow

specs:
  C программистским запросом:
    Первая страница:
      - do: получить выдачу по запросу "css max lines"
      - assert: |
          на странице присутствует колдунщик кода в правой колонке
          на странице присутствует пейджер (переключатель страниц)
      - screenshot: внешний вид пейджера [first_page]
      - do: нажать на элемент "i++", дождаться загрузки страницы
      - tech: сработал счётчик path="/$page/$main/$pager/button-next"
      - assert: |
          на странице присутствует пейджер
          в адресной строке стоит флаг p=1
          в адресной строке стоит флаг cee=1
      - screenshot: внешний вид пейджера [second_page]
      - do: нажать на элемент "3", дождаться загрузки страницы
      - tech: сработал счётчик path="/$page/$main/$pager/button[@pageno=3]"
      - assert: |
          на странице присутствует пейджер
          в адресной строке стоит флаг p=3
          в адресной строке стоит флаг cee=1
      - screenshot: внешний вид пейджера [fourth_page]

files:
  - features/pager/pager@desktop.hermione.js

v-team: Beauty

tlds: all
