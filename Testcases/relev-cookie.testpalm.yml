feature: Кука релевантности

specs:
  Переключить элементы управления:
    - do: получить выдачу по запросу 'Тест'
    - assert: на выдаче есть тумблер переключения показа куки

    - do: кликнуть на тумблер перелючения показа куки
    - assert: показан блок управления кукой
    - assert: показан элемент куки для выдачи
    - assert: показан номер документа
    - assert: нет js-ошибок

    - do: кликнуть на тумблер перелючения показа куки
    - assert: спрятан блок управления кукой
    - assert: спрятаны элементы куки для выдачи
    - assert: спрятаны номера документов
    - assert: нет js-ошибок

v-team: Common

tags:
  - no_assessors

files:
  - features/common/relev-cookie/relev-cookie.hermione.js

tlds: none

browsers:
  notFor:
    - searchapp-phone
