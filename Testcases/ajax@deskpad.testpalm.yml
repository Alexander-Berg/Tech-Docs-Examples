feature: Перезапросы

specs:
  Изменение версии статики:
    - do: получить выдачу по запросу 'crimea'
    - tech: выполнить в консоли `BEM.blocks['i-global'].param('static-host', 111)`
    - tech: выполнить в консоли `BEM.blocks['serp-request'].navigateTo('?text=yalta')`
    - assert: страница полностью перезагрузилась
  Запрос в ссылках поиск в других системах:
    - do: получить выдачу по запросу 'yalta'

files:
  - features/ajax/ajax@deskpad.hermione.js

priority: high

tags:
  - no_assessors

v-team: Velocity

tlds: none
