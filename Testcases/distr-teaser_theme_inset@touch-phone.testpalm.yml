feature: Дистрибуционный тизер
type: Inset

params:
  foreverdata: '1194514100'

specs:
  beforeEach:
    - do: получить выдачу по запросу 'foreverdata'
    - assert: появился дистрибуционный тизер на выдаче
  Внешний вид inset horizontal:
    - label: TeaserIns-{{SET_NAME}}
    - screenshot: внешний вид [plain]
    - do: повернуть устройство в ландшафтную (горизонтальную) ориентацию
    - screenshot: пример внешнего вида в ландшафтной ориентации [horizontal]

files:
  - features/distr-teaser/distr-teaser_theme_inset@touch-phone.hermione.js

tags:
  - skip_e2e_check
  - distribution

v-team: Distribution

tlds:
  - ru
  - by

browsers:
  notFor:
    - searchapp-phone
