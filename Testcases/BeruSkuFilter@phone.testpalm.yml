feature: beruSkuFilter

specs:
  Внешний вид блока:
    - do: открыть страницу (https://yandex.ru/turbo?stub=beruskufilter/default.json)
    - assert: должны быть видны кнопки переключения sku
    - screenshot: внешний вид соответствует скриншоту [plain]

  Внешний вид блока, только текст:
      - do: открыть страницу (https://yandex.ru/turbo?stub=beruskufilter/text.json)
      - assert: должны быть видны кнопки переключения sku, только текстовые
      - screenshot: внешний вид соответствует скриншоту [text]

  Sku фильтры должны проматываться в iframe:
    - params:
        foreverdata: '1264387472'
    - do: получить выдачу по запросу 'foreverdata' и кликнуть в заголовок первого сниппета (начинается с "Тестовая страница для BeruRecommendations swipe check")
    - assert: открывается турбо-страница
    - do: найти блок с фильтрами
    - assert: должен быть виден блок с фильтрами
    - do: свайпнуть
    - assert: контент блока должен проскролится
    - screenshot: внешний вид соответствует скриншоту [iframe-scroll]

files:
  - platform/components/BeruSkuFilter/__tests__/BeruSkuFilter@phone.hermione.js

v-team: Beru

manager: seoshnik

tlds: none
