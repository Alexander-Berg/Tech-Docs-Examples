feature: Одна организация
type: Горнолыжный курорт

specs:
  Карточки услуг – внешний вид и клики:
    - params:
        foreverdata: '2772065109'
    - do: получить выдачу по запросу 'foreverdata'
    - assert: на выдаче есть врезка с карточками в правой колонке
    - assert: при наведении на карточку меняется цвет фона
    - screenshot: внешний вид карточек [OrgTile]
    - do: кликнуть в карточку 'Авиабилеты'
    - assert: ссылка открылась в новой вкладке
    - tech: сработал счётчик /$page/$parallel/$result/composite/tabs/about/ski-services/avia
    - do: кликнуть в карточку 'Отели'
    - assert: ссылка открылась в той же вкладке
    - tech: сработал счётчик /$page/$parallel/$result/composite/tabs/about/ski-services/hotels

  Карточки услуг – мало карточек (без билетов и отелей):
    - params:
        foreverdata: '1002135799'
    - do: получить выдачу по запросу 'foreverdata'
    - assert: на выдаче есть врезка с двумя карточками в правой колонке
    - screenshot: внешний вид карточек [OrgTile2]

files:
  - src/features/Companies/Companies.features/OrgTile/OrgTile.test/OrgTile@desktop.hermione.js

tags:
  - skip_e2e_check

v-team: Freshness
tlds: ru
