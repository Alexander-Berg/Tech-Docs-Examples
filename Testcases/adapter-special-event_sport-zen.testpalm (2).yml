feature: Специальные события
type: Дзен в спорте

specs:
  Карусели:
    Карточки с видео:
      params:
        foreverdata: '2651598289'
      beforeEach:
        - do: получить выдачу по запросу 'foreverdata'
        - assert: получена выдача по указанному запросу
        - assert: на выдаче присутствует  колдунщик НХЛ
      Внешний вид:
        - screenshot: примерный внешний вид [video]
        - assertMetrics: '*'
      Клики:
        - do: кликнуть в видео в первой карточке в карусели Видео
        - assert: в новой вкладке открылся сервис Яндекс.Эфир
        - tech: сработал счетчик "/$page/$top/$result/video/link"
        - do: кликнуть в текст в первой карточке в карусели Видео
        - assert: в новой вкладке открылся сервис Яндекс.Эфир
        - tech: сработал счетчик "/$page/$top/$result/video/link"
        - assertMetrics: '*'
    Карточки с изображениями:
      params:
        foreverdata: '2651598289'
        user_time: '201908271005'
      beforeEach:
        - do: получить выдачу по запросу 'foreverdata'
        - assert: получена выдача по указанному запросу
        - assert: на выдаче присутствует  колдунщик НХЛ
      Внешний вид:
        Карточка с картинкой и с иконкой:
          - screenshot: примерный внешний вид первой карточки [pic-with-icon]
          - assertMetrics: '*'
        Карточка с картинкой без иконки:
          - do: проскроллить влево до второй карточки
          - screenshot: примерный внешний вид второй карточки [pic]
          - assertMetrics: '*'
        Карточка с логотипом канала и с иконкой:
          - do: проскроллить влево до третьей карточки
          - screenshot: примерный внешний вид третьей карточки [logo-with-icon]
          - assertMetrics: '*'
        Карточка с логотипом канала без иконки:
          - do: проскроллить влево до четвертой карточки
          - screenshot: примерный внешний вид четвертой карточки [logo]
          - assertMetrics: '*'
      Клики:
        Карточка c иконкой:
          - do: кликнуть в картинку в первой карточке в карусели новостей и рекомендаций
          - assert: в новой вкладке открылся пост на сервисе Яндекс.Дзен
          - tech: сработал счетчик "/$page/$top/$result/news/link"
          - do: кликнуть в текст в первой карточке в карусели новостей и рекомендаций
          - assert: в новой вкладке открылся пост на сервисе Яндекс.Дзен
          - tech: сработал счетчик "/$page/$top/$result/news/link"
          - assertMetrics: '*'
        Карточка без иконки:
          - do: кликнуть в картинку во второй карточке в карусели новостей и рекомендаций
          - assert: в новой вкладке открылся сайт sportbox.ru
          - tech: сработал счетчик "/$page/$top/$result/news/link"
          - do: кликнуть в текст во второй карточке в карусели новостей и рекомендаций
          - assert: в новой вкладке открылся сайт sportbox.ru
          - tech: сработал счетчик "/$page/$top/$result/news/link"
          - assertMetrics: '*'
    Ссылка "Еще новости":
      params:
        foreverdata: '2396651016'
      beforeEach:
        - do: получить выдачу по запросу 'foreverdata'
        - assert: на выдаче присутствует колдунщик ФК Спартак
        - do: проскроллить карусель новостей влево до конца
        - assert: в конце карусели отобразилась ссылка "Ещё новости"
      Внешний вид:
        - screenshot: примерный внешний вид карусели [more-item]
        - assertMetrics: '*'
      Клик:
        - do: тапнуть по ссылке "Ещё новости"
        - assert: в новой вкладке открылся сервис Яндекс.Новости
        - tech: сработал счетчик "/$page/$top/$result/news/more"
        - assertMetrics: '*'
    Карточки с Турбо-ссылками:
      params:
        foreverdata: '220676070'
      beforeEach:
        - do: получить выдачу по запросу 'foreverdata'
        - assert: получена выдача по указанному запросу
        - assert: на выдаче присутствует колдунщик НХЛ
      Внешний вид:
        - screenshot: примерный внешний вид карточки [turbo]
        - assertMetrics: '*'
      Клики - новая вкладка:
        - browsers:
          - searchapp-phone
        - do: кликнуть в картинку в первой карточке в карусели новостей и рекомендаций
        - assert: открылась турбо-страница (содержание страницы может не соответствовать ссылке)
        - tech: сработал счетчик "/$page/$top/$result/news/link"
        - assertMetrics: '*'
    Карточки с видео для сайдблока:
      params:
        foreverdata: '3331507429'
      beforeEach:
        - do: получить выдачу по запросу 'foreverdata'
        - assert: получена выдача по указанному запросу
        - assert: на выдаче присутствует  колдунщик ФНЛ
      Внешний вид:
        - screenshot: примерный внешний вид [sideblock-videos]
        - assertMetrics: '*'
      Клики:
        - do: кликнуть в видео в первой карточке в карусели видео
        - assert: в сайдблоке открылся сервис Яндекс.Эфир
        - tech: сработал счетчик "/$page/$top/$result/video/video"
        - do: кликнуть в текст в первой карточке в карусели видео
        - assert: в сайдблоке открылся сервис Яндекс.Эфир
        - tech: сработал счетчик "/$page/$top/$result/video/link"
        - do: кликнуть в видео во второй карточке в карусели видео
        - assert: в сайдблоке открылся сервис Яндекс.Эфир
        - tech: сработал счетчик "/$page/$top/$result/video/video"
        - do: кликнуть в текст во второй карточке в карусели Видео
        - assert: в сайдблокеоткрылся сервис Яндекс.Эфир
        - tech: сработал счетчик "/$page/$top/$result/video/link"
        - assertMetrics: '*'

tlds:
  - ru
  - by
  - uz
  - kz
  - uz

tags:
  - sport
  - reviewed
  # эксперимент
  - skip_e2e_check

files:
  - features/touch-phone/adapter-special-event/adapter-special-event_type_sport/zen/adapter-special-event_sport-zen.hermione.js

v-team: Sport
