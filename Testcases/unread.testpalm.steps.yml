Проверка непрочитанных в сборке widget:
    - do: выставить параметр конструктора badgeCount=0
    - do: выставить параметр конструктора badgeType=dot
    - do: выставить параметр конструктора theme=light
    - screenshot: на кнопке виджета отсутствует бадж с непрочитанными [no-unread-widget]
    - do: выставить параметр конструктора badgeCount=1
    - screenshot: на кнопке виджета виден бадж с непрочитанными [unread-light-dot-widget]
    - do: выставить параметр конструктора theme=dark
    - screenshot: на кнопке виджета виден бадж с непрочитанными [unread-dark-dot-widget]
    - do: выставить параметр конструктора theme=legacy
    - screenshot: на кнопке виджета виден бадж с непрочитанными [unread-legacy-dot-widget]
    - do: выставить параметр конструктора badgeType=num
    - do: выставить параметр конструктора theme=light
    - screenshot: на кнопке виджета виден бадж с непрочитанными [unread-light-num-ltmax-widget]
    - do: выставить параметр конструктора theme=dark
    - screenshot: на кнопке виджета виден бадж с непрочитанными [unread-dark-num-ltmax-widget]
    - do: выставить параметр конструктора theme=legacy
    - screenshot: на кнопке виджета виден бадж с непрочитанными [unread-legacy-num-ltmax-widget]
    - do: выставить параметр конструктора badgeCount=100
    - do: выставить параметр конструктора theme=light
    - screenshot: на кнопке виджета виден бадж с непрочитанными [unread-light-num-gtmax-widget]
    - do: выставить параметр конструктора theme=dark
    - screenshot: на кнопке виджета виден бадж с непрочитанными [unread-dark-num-gtmax-widget]
    - do: выставить параметр конструктора theme=legacy
    - screenshot: на кнопке виджета виден бадж с непрочитанными [unread-legacy-num-gtmax-widget]

Проверка непрочитанных в сборке widget_ya:
    - do: выставить параметр конструктора badgeCount=0
    - do: выставить параметр конструктора badgeType=dot
    - do: выставить параметр конструктора theme=light
    - screenshot: на кнопке виджета отсутствует бадж с непрочитанными [no-unread-widget_ya]
    - do: выставить параметр конструктора badgeCount=1
    - screenshot: на кнопке виджета виден бадж с непрочитанными [unread-light-dot-widget_ya]
    - do: выставить параметр конструктора theme=dark
    - screenshot: на кнопке виджета виден бадж с непрочитанными [unread-dark-dot-widget_ya]
    - do: выставить параметр конструктора theme=legacy
    - screenshot: на кнопке виджета виден бадж с непрочитанными [unread-legacy-dot-widget_ya]
    - do: выставить параметр конструктора badgeType=num
    - do: выставить параметр конструктора theme=light
    - screenshot: на кнопке виджета виден бадж с непрочитанными [unread-light-num-ltmax-widget_ya]
    - do: выставить параметр конструктора theme=dark
    - screenshot: на кнопке виджета виден бадж с непрочитанными [unread-dark-num-ltmax-widget_ya]
    - do: выставить параметр конструктора theme=legacy
    - screenshot: на кнопке виджета виден бадж с непрочитанными [unread-legacy-num-ltmax-widget_ya]
    - do: выставить параметр конструктора badgeCount=100
    - do: выставить параметр конструктора theme=light
    - screenshot: на кнопке виджета виден бадж с непрочитанными [unread-light-num-gtmax-widget_ya]
    - do: выставить параметр конструктора theme=dark
    - screenshot: на кнопке виджета виден бадж с непрочитанными [unread-dark-num-gtmax-widget_ya]
    - do: выставить параметр конструктора theme=legacy
    - screenshot: на кнопке виджета виден бадж с непрочитанными [unread-legacy-num-gtmax-widget_ya]
