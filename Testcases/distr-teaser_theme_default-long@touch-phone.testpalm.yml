feature: Дистрибуционный тизер
type: Default c длинным заголовоком

params:
  foreverdata: '3314013098'

specs:
  beforeEach:
    - do: получить выдачу по запросу 'foreverdata'
    - assert: появился дистрибуционный тизер на выдаче
  Внешний вид default-long horizontal:
    - screenshot: внешний вид [plain]
    - do: повернуть устройство в ландшафтную (горизонтальную) ориентацию
    - screenshot: пример внешнего вида в ландшафтной ориентации [horizontal]

files:
  - features/distr-teaser/distr-teaser_theme_default-long@touch-phone.hermione.js

v-team: Distribution

tags:
  - skip_e2e_check
  - distribution

tlds:
  - ru
  - by

browsers:
  notFor:
    - searchapp-phone
